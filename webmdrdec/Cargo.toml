[package]
name = "webmdrdec"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.52.3", features = ["rt", "io-util", "net", "macros", "fs"] }

[build-dependencies]
wasm-bindgen-cli-support = "0.2.122"
